# Add project specific ProGuard rules here.
# https://developer.android.com/topic/performance/app-startup

# Room, Hilt, Ktor, kotlinx.serialization all ship consumer proguard rules —
# nothing custom needed yet. Add rules here if release-build minification
# breaks reflection-based code (e.g. serialization models) later.
